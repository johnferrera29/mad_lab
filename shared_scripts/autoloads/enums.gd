extends Node
## Autoload that contains globally accessible and shared enums.


## Different scaling modes used by [ScaleGun].
enum ScaleMode {
	SHRINK,
	ENLARGE,
	RESET,
}

## Different weapon types the player can use.
enum WeaponType {
	SCALE_GUN,
	BOMB_LAUNCHER,
	FREEZE_RAY,
}
